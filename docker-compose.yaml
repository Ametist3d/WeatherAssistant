services:
  weather-assistant:
    build:
      context: .
      args:
        VITE_API_URL: ${VITE_API_URL:-}
    container_name: weather-assistant
    ports:
      - "${APP_PORT:-8000}:8000"
    env_file:
      - .env
    restart: unless-stopped